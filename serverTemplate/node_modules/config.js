// this is a shortcut to the environment configuration (so you can do require('config') instead of require('somePath/to/configuration')

var utils = require('bt/utils')

utils.merge(exports,require("./../../configuration"))