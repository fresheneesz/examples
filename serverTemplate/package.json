{
  "name": "entefyInstaller",
  "version": "0.0.1",
  "readme": "This allows npm install to install entefy. Use `npm install` from the git directory to install the application. Don't add any dependencies into this package.json file, instead add them to installComponents/package.json .",
  "dependencies": {
  },
  "scripts": {
    "preinstall": "node preinstall.js"
  }
}
